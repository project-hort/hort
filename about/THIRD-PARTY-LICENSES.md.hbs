{{!-- Rendered by `cargo about generate` (see `scripts/regenerate-attribution.sh`
    and `about.toml`). Iterates `licenses[].used_by[]` (one row per
    resolved crate+license-text pair, not `crates[]`) so the SPDX id and
    text shown are what was actually resolved for that crate, not its raw
    (possibly compound, e.g. "MIT OR Apache-2.0") Cargo.toml `license`
    field. The #if crate.source guard excludes hort's own workspace
    members — their `source` is null (path dependency), unlike every real
    registry/git dependency. See `about.toml`'s [private] comment for why
    this template-level filter exists instead of cargo-about's built-in
    (publish-based) private-crate skip. --}}
# Third-Party Licenses

This document is generated by `cargo about` (see `scripts/regenerate-attribution.sh`)
from the dependency graph of hort's shipped binaries (`hort-server`, `hort-worker`,
`hort-cli`). It lists every third-party crate compiled into those binaries, the
license under which it is used, and the full license text. hort's own crates are
not included — see `LICENSE-MIT` / `LICENSE-APACHE` for hort's license.

Do not edit this file by hand; run `scripts/regenerate-attribution.sh` and commit
the result instead.

## Components

| Crate | Version | License | Repository |
| --- | --- | --- | --- |
{{#each licenses~}}
{{#each used_by~}}
{{#if crate.source~}}
| {{crate.name}} | {{crate.version}} | {{../id}} | {{#if crate.repository}}{{crate.repository}}{{else}}(not specified){{/if}} |
{{/if~}}
{{/each~}}
{{/each}}

## License texts

{{#each licenses~}}
{{#each used_by~}}
{{#if crate.source~}}
### {{crate.name}} {{crate.version}} — {{../id}}

Repository: {{#if crate.repository}}{{crate.repository}}{{else}}(not specified){{/if}}

```text
{{../text}}
```

{{/if~}}
{{/each~}}
{{/each}}
