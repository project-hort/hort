# PermissionGrant: [developer, ci-pusher] -> read maven-trivy-e2e.
#
# maven-trivy-e2e is private (isPublic: false): the scenario pulls the
# fixture JAR through it as dev-user, so without this grant the pull returns
# 403 before it ever reaches the quarantine/scan gate the scenario asserts
# on. Two-claim subject per the allowlist linter, mirroring
# dev-read-crates-proxy.yaml.
apiVersion: project-hort.de/v1
kind: PermissionGrant
metadata:
  name: dev-read-maven-trivy-e2e
spec:
  subject:
    kind: claims
    required: [developer, ci-pusher]
  permission: read
  repository: maven-trivy-e2e
