# ClaimMapping: IdP group `hort-admins` resolves to the synthetic
# `admin` claim (RbacEvaluator short-circuits on it).
apiVersion: project-hort.de/v1
kind: ClaimMapping
metadata:
  name: admins
spec:
  idpGroup: hort-admins
  claim: admin
