apiVersion: project-hort.de/v1beta1
kind: ScanPolicy
metadata:
  name: cargo-e2e-permissive
spec:
  # Per-repo operator opt-out of quarantine-by-default. See
  # `pypi-e2e-permissive.yaml` for the full rationale — the cargo native
  # test (`scripts/native-tests/scenarios/clients/cargo.sh`) follows the same
  # publish-then-immediately-pull pattern.
  scope:
    repository: cargo-e2e
  severityThreshold: critical
  quarantineDuration: 0s
  requireApproval: false
  # Provenance off for permissive e2e (no per-ingest verify job).
  provenanceMode: off
  scanBackends: []
  licensePolicy: {}
