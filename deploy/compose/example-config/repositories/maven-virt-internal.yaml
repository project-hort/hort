apiVersion: project-hort.de/v1beta1
kind: ArtifactRepository
metadata:
  name: maven-virt-internal
spec:
  name: "Maven Virtual — private member"
  description: "Private hosted member of maven-virt-e2e (ADR 0031); scripts/native-tests/scenarios/clients/maven-virtual.sh"
  format: maven
  type: hosted
  storage:
    backend: filesystem
    path: /var/lib/hort-server/cas/maven-virt-internal
  isPublic: false
  replicationPriority: local_only
