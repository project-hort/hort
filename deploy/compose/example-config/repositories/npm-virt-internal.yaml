apiVersion: project-hort.de/v1beta1
kind: ArtifactRepository
metadata:
  name: npm-virt-internal
spec:
  name: "npm Virtual — private member"
  description: "Private hosted member of npm-virt-e2e (ADR 0031); scripts/native-tests/scenarios/clients/virtual.sh"
  format: npm
  type: hosted
  storage:
    backend: filesystem
    path: /var/lib/hort-server/cas/npm-virt-internal
  isPublic: false
  replicationPriority: local_only
