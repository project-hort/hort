server {
    listen 80;
    server_name localhost;

    location /rpm/ {
        alias /usr/share/nginx/html/rpm/;
        autoindex on;
    }

    location /deb/ {
        alias /usr/share/nginx/html/deb/;
        autoindex on;
    }
}
